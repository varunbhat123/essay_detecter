class EssayService:
    """Placeholder service for essay detection logic."""

    async def analyze(self, text: str) -> dict[str, object]:
        return {
            "score": 0.0,
            "label": "pending",
            "explanation": "Detection logic will be implemented in a future phase.",
            "text_length": len(text),
        }
